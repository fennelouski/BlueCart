# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BlueRecipes' do
    use_frameworks!
    platform :ios, '10.0'
    use_frameworks!
    `$(inherited)`
    pod ‘Alamofire’
    pod 'Pastel'
    pod 'PureLayout'
    pod 'Unbox'
    inherit! :search_paths
end
